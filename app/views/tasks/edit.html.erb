<h1><%= @task.title %> - <%= @task.description %></h1>

<div>
  <%= form_for @task, url: edit_path(@task) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :description %>
    <%= f.text_field :description %>
    <%= f.submit %>
  <% end %>
</div>
